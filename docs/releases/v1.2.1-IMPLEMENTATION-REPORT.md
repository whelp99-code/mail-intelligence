# Mail Intelligence v1.2.1 구현 보고서

## 판정

```text
Rules Provider                         PASS
OpenAI Codex ChatGPT OAuth integration PASS
xAI Grok OAuth integration contract    PASS
LM Studio/F-AIOS/Gemini active paths   REMOVED
OAuth token direct storage             0건
Provider-to-provider fallback          0건
Mail mutation                          DISABLED
```

## 구현

- `src/ai/oauth-cli-provider.js`
  - 공식 CLI 탐색
  - OAuth 상태 확인
  - 격리된 structured execution
  - tool event fail-closed
  - timeout·출력 제한·환경변수 allowlist
- `schemas/mail-analysis.schema.json`
- OAuth Provider 상태·안내·합성 테스트 API
- OAuth Provider 설정 UI
- 사용자 운영 명령
  - `npm run oauth:status`
  - `npm run oauth:instructions`
  - `npm run oauth:enable -- <provider>`
- 레거시 Provider 설정의 Rules 이관과 fail-closed 거부

## 현재 Ubuntu 상태

```text
Codex CLI               installed
Codex CLI version       0.144.6
Codex auth               ChatGPT OAuth
Grok CLI                 installed
Grok CLI version         1.0.5
Grok auth                xAI OAuth
OAuth external-AI gate  enabled
Main service             active / running
Tailnet service          active / running
Outlook stored mail      359
```

실제 Codex·Grok 모델 생성 합성 테스트는 실행 도구가 별도 네트워크 승인을 요구하여 `NOT RUN`이다. 공식 CLI 설치와 OAuth 인증 상태는 live API에서 확인했다. Fake official-CLI contract, 전체 API 경로, 출력 Schema, Evidence 검증은 자동 테스트로 검증한다.

## 검증

```text
npm run verify:v1.2.1       PASS
node:test                    168/168 PASS
OAuth focused tests         7/7 PASS
Precision fixtures          20/20 PASS
Precision assertions        77/77 PASS
ESLint / HTML / CSS          PASS
Health / Safety             PASS
npm audit                   0 vulnerabilities
Fresh working copy          PASS
Live local deployment       PASS
Tailnet exposure            PASS
```

실서비스 상태:

```text
SQLite schema               4
SQLite quick_check          ok
Mail folders                20
Stored messages             359
Precision classifications   359
Review required             156
External mail send          blocked
```

## 안전선

```text
Outlook scope           User.Read Mail.Read
Mail.Send               absent
Mail.ReadWrite          absent
External AI             operator gate + UI consent
Token ownership         official CLI
Token DB storage        none
Rules fallback          yes
Cross-provider fallback no
```
