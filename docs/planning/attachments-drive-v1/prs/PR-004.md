상태: 조사 단계 / 결정 아님 / 새 첨부 기능 실행·측정 미실시

# PR004 — Google 계정·Picker·파일 가져오기·변환

TASK R10~14. SCOPE MI. CREATE `src/adapters/google-drive-client.js`, `src/application/mail-drive-api.js`, `src/application/mail-drive-connections.js`, `test/google-drive-client.test.js`, `test/mail-drive-api.test.js`, `test/mail-drive-oauth.test.js`, 다음 migration `migrations/007_mail_drive_connections.sql`. MODIFY server.mjs, send-review.js, mail-attachment-assets.js, mail-send-api.js, 기존 config loader 및 migration registry 필요 부분. FORBIDDEN Outlook 인증 변경, 토큰을 Gateway에 저장, 임의 URL 다운로드, Drive 쓰기·공유 변경.

DELIVERABLE `createGoogleDriveClient`(getMetadata/download/exportFile), `createMailDriveApi`, `createDriveConnectionService`; 03 endpoint 제공. 확정 migration 경로 `migrations/007_mail_drive_connections.sql`; 자동 탐색 로더는 수정하지 않는다. 환경 설정은 `server.mjs`에 연결하고 UI 파일 `src/index.html`·`src/styles.css`도 수정 범위에 포함한다.

DETAIL A OAuth: 기존 앱 세션/비밀 저장 구조 확인 → state/PKCE/nonce/replay synthetic 테스트 → 계정별 암호화 refresh token → 고정 callback → disconnect·revoked 처리. 클라이언트 등록값은 환경 입력으로만 받는다. 실제 OAuth console 접근/동의는 사용자 승인 없으면 실행하지 않는다.

DETAIL B 다운로드: 고정 Google API origin·timeout·redirect 거절·bounded reader → metadata 전후 일치 → blob download 또는 MIME allowlist export → scanner → 동일 PC 자산 서비스에 저장. export 크기 미상도 stream 누적 제한을 적용한다. source 변경/권한 상실/429/timeout은 오류로 종료하며 request_id로 이미 완료한 사본은 중복 생성하지 않는다.

DETAIL C UI: 연결 상태·계정 표시·Picker 단건/최대5 선택 → 사람 전용 selection API로 허용 기록 저장 → 파일/링크 모드 선택 → Google 문서 PDF/Office 선택 → import 진행·오류·취소. browser access token은 저장 금지. import 취소는 공유 자산을 삭제하지 않고 해당 요청만 종료한다. 선택하지 않은 provider 접근 가능 ID도 import403, 다른 계정 grant 재사용403, disconnect 후 import403을 시험한다.

DETAIL D 승인 전 Drive metadata/권한 재확인을 주입한다. source version 변경·삭제·접근 거부는 전송0과 새 가져오기 안내. 재확인은 사본 bytes를 최신으로 바꾸지 않는다.

VERIFY `node --test test/google-drive-client.test.js test/mail-drive-api.test.js test/mail-drive-oauth.test.js test/mail-attachment-send.test.js`; `npm run lint`; `npm run validate:html`; `npm run validate:css`. fake Google 응답 기반. live Picker/OAuth/export 검증은 PR006 별도 승인 단계다.
