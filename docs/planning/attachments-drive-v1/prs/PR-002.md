상태: 조사 단계 / 결정 아님 / 새 첨부 기능 실행·측정 미실시

# PR002 — 검사·보존·복원

TASK: R02~04/R16~17을 fail-closed 운영 경계로 구현한다.

SCOPE MI. CREATE `src/adapters/attachment-scanner.js`, `src/application/attachment-policy.js`, `scripts/mail-attachment-retention.mjs`, `test/attachment-policy.test.js`, `test/attachment-retention.test.js`, `test/attachment-backup.test.js`. MODIFY PR001 assets/crypto/API, `src/storage/backup-restore.js`. FORBIDDEN 실제 파일 삭제·검사기 설치·운영 키 생성·기존 백업 일괄 변경.

DELIVERABLE: `validateAttachment`, `scanAttachment`, `planAttachmentRetention` 함수와 dry-run CLI. 새 parser 의존성이 필요하면 라이선스·버전·동작을 확인하고 package.json/lock만 추가 범위로 기록한다. 무검증 자체 parser로 허용 판정을 하지 않는다.

DETAIL A: MIME/signature, Office ZIP 한도, HWP 지원 불가 경로, executable/매크로/암호화 거절 테스트부터 작성한다. scanner spawn은 shell:false, timeout15초, 출력 크기 제한64KiB, 임시 경로0700/파일0600, finally 정리. 실제 malware fixture 대신 주입 scanner 반환값으로 악성 판정을 시험한다.

DETAIL B: 트랜잭션 quota 예약·abort 반환·동시2 제한을 검증한다. 정리 CLI는 기본 dry-run, `--apply`를 명시한 경우에도 confirmed approved/sending 및 미만료 참조 자산은 제외한다. 30일 기준은 sent_at/cancelled_at/failed_at이며 실패 상태가 불확실 sending이면 제외한다. 메타데이터·감사행은 유지한다. 운영 --apply는 승인 없으면 실행 금지.

DETAIL C: 암호화 DB snapshot과 별도 키 복구 절차를 작성하고 격리된 DB 복원으로 모든 보존 자산 hash를 대사한다. 기존 백업은 삭제하지 않는다. 키 누락/변조/만료bytes가 있는 경우 구분해 실패 보고한다. 과거 encryption_aad_version/encryption_policy_version으로 암호화한 자산을 정책 갱신 뒤 복원·복호화하고, 현재 scan_policy_version 재검사 실패 시 승인 차단을 assert한다. 암호화 AAD를 현재 정책 값으로 대체하지 않는다.

VERIFY: `node --test test/attachment-policy.test.js test/attachment-retention.test.js test/attachment-backup.test.js test/mail-attachment-assets.test.js`; `npm run verify:backup:isolated`; `npm run lint`. 신규 테스트는 이 카드가 생성한다. 운영 scanner 설치 성공은 테스트 double PASS와 별도 상태로 보고한다.
