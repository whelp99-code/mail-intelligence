상태: 조사 단계 / 결정 아님 / 새 첨부 기능 실행·측정 미실시

# PR006 — 통합 인수·운영 인계

TASK R17~18 및 전체 R01~18 증거 대사. SCOPE MI. CREATE `test/mail-attachments-e2e.test.js`, `docs/operations/mail-attachments-runbook.md`, `artifacts/attachments-acceptance/README.md`(합성 결과만). MODIFY 새 기능 테스트에서 발견한 결함 파일만 책임 범위를 보고하고 수정한다. FORBIDDEN 게이트 삭제, 성공으로 skip 처리, 비밀·실메일 artifact 커밋, 무승인 배포·발송.

DETAIL: 06 표를 테스트 ID에 매핑 → 격리 DB migration/restore → full release commands → 실제 브라우저 합성 UI → 운영 설정 안내. 별도 운영 승인 후에만 backup→migration→flagOFF health→자기 소유 합성 파일 시험→owner 확인 순서로 진행한다. live 전송은 사용자에게 수신인/파일명/크기/hash/본문을 보여 승인받은 한 건만 보낸다. 종료 후 flag를 원래 값으로 되돌린다.

VERIFY `node --test test/mail-attachments-e2e.test.js`; `npm test`; `npm run lint`; `npm run validate:html`; `npm run validate:css`; `npm run verify:backup:isolated`; `npm run verify:v1.2.2`. Gateway 결과는 PR005 보고서로 참조하고 이 카드가 다른 저장소를 수정하지 않는다. 마지막 증거는 COMMIT/TEST/UI/DEPLOY/SEND/RECEIVE를 각각 분리한다.
