상태: 조사 단계 / 결정 아님 / 새 첨부 기능 실행·측정 미실시

# PR001 — PC 첨부와 승인·발송 핵심

TASK: R01~08의 소형 첨부 경로를 합성 환경에서 구현한다. 운영 flag는 OFF 유지한다.

SCOPE: MI 저장소만. CREATE `src/application/mail-attachment-assets.js`, `src/application/mail-attachment-api.js`, `src/storage/mail-attachment-crypto.js`, `test/mail-attachment-assets.test.js`, `test/mail-attachment-api.test.js`, `test/mail-attachment-send.test.js`, `test/mail-attachment-ui.test.js`; migration은 기존 migration 디렉터리의 다음 번호에 `migrations/006_mail_attachments.sql`을 생성한다. MODIFY `src/application/mail-send-drafts.js`, `src/application/mail-send-api.js`, `src/adapters/microsoft-graph-send.js`, `src/send-review.js`, `server.mjs` 및 기존 migration registry(현재 로더가 자동 탐색이면 변경하지 않음). 검토 HTML/CSS의 실제 파일은 `send-review.js` 로딩 위치를 검색해 1개씩 매핑하고 UI만 수정한다. FORBIDDEN Gateway, 수신메일 분류, OAuth scope, 운영 파일.

DELIVERABLE: `createAttachmentAssetService`, `createMailAttachmentApi`, `encryptAttachment/decryptAttachment` export; API/스키마는 03 준수. 확정 파일 매핑: migration은 `migrations/006_mail_attachments.sql`, UI는 `src/index.html`·`src/styles.css`. `src/storage/sqlite-store.js`가 migration을 자동 탐색하므로 registry 변경은 없다. 위 설명의 migration 이름보다 이 확정 매핑을 우선한다.

DETAIL SUB001-A: 기존 migration loader/crypto/config를 읽고 증거 경로를 기록한다. 신규 schema와 crypto roundtrip/변조/다른 AAD 실패 테스트를 먼저 작성한다. raw upload bounded stream, request_id replay, 소유권, quota reservation을 구현한다. scanner는 주입 인터페이스로 두고 production 기본은 unavailable이며 합성 테스트만 fake PASS를 사용한다.

DETAIL SUB001-B: 기존 createDraft/approve/claim 동작을 읽는다. 자산 FK·순서·v2 canonical digest를 구현한다. 비어 있는 첨부/링크는 v1을 유지한다. before-claim 검증 버퍼를 sendOnce에 전달하며 삭제/변조 race, 중복 approve를 시험한다. 기존 API 응답 상태와 충돌 시 03 error를 어댑터에서 매핑한다.

DETAIL SUB001-C: Graph payload attachments와 read-only receipt hash 대사를 구현한다. Graph 응답202·timeout·추가첨부·누락·bytes 불일치·pagination을 mock한다. 동일 request 재실행 시 send POST 횟수1을 assert한다. HTTP retry 공통 로직을 재사용해 send를 재시도하지 않는다.

DETAIL SUB001-D: 파일 picker/upload 상태/다운로드 검토/제거/새 초안 동작을 구현한다. 기존 저장 초안의 파일 교체는 금지한다. keyboard·escaped filename·초과 오류·대기 중 저장 방지를 검증한다.

VERIFY: 새 파일 생성 후 `node --test test/mail-attachment-assets.test.js test/mail-attachment-api.test.js test/mail-attachment-send.test.js test/mail-attachment-ui.test.js test/mail-send-drafts.test.js test/mail-send-api.test.js test/mail-send-http.test.js test/microsoft-graph-send.test.js`; 이어 `npm run lint`. 실패0, skip0. 기존 text-only digest fixture 동일, 무권한 발송0. ui/grok-bot 자산 정상 사용과 교차 source 거절을 각각 assert한다. 실제 scanner·운영 UI 수신검증은 PR002/006에서 하며 이 단계에 완료로 표시하지 않는다.
