상태: 조사 단계 / 결정 아님 / 새 첨부 기능 실행·측정 미실시

# PR003 — Drive 링크

TASK R09. SCOPE MI. CREATE `src/application/drive-links.js`, `test/drive-links.test.js`; MODIFY mail-send-drafts.js/mail-send-api.js/send-review.js(각 기존 src 경로). FORBIDDEN Drive OAuth·permissions API·URL fetch·파일 bytes 추정.

DELIVERABLE `parseDriveLink`, `renderDriveLinks`; 03의 허용 URL·digest 계약을 따른다.

DETAIL: parser 정상/악성 host/userinfo/query/CRLF 테스트 → 정확한 링크 본문 렌더링 → draft v2와 acknowledged 플래그 검증 → 링크 추가/제거·권한 미확인 경고 UI. 저장된 링크 변경은 새 request_id와 재승인을 요구한다. 승인 화면과 전송 본문이 동일한지 assert한다. 링크 모드는 첨부 수신 검증 통과로 세지 않는다.

VERIFY `node --test test/drive-links.test.js test/mail-send-drafts.test.js test/mail-attachment-send.test.js`; `npm run lint`. 테스트에서 모든 네트워크 fetch 호출0, permissions mutation0.
